/**
 * _islower - Determine if is caracter is lowercase
 * @c: The caracter we want to Know
 *
 * Return: 1 if the caracter is lowercase and 0 otherwise
 */
int _islower(int c)
{
if (c > 96  && c < 123)
return (1);
	return (0);
}
